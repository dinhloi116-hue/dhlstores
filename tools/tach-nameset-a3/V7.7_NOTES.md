# V7.7

- Multi-frame nesting: creates physical frame rectangles on the Corel canvas instead of resizing the page.
- Automatically continues into Frame 2, Frame 3, etc. when the previous frame is full.
- Preset buttons show a selected state and all buttons have press feedback.
- Outline UI simplified to explicit outline distance, cutline width, white border checkbox and cutline checkbox.
- Transparent bitmap trace path updated to use TraceSettings properties before Finish().
- Outline distance uses an outside contour in millimeters.
- Image/vector + generated white border/cutline are grouped automatically.
- Keeps the name/number 30 mm alignment fix by temporarily switching document units to millimeters.
