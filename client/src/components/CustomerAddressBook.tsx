import { useMemo, useState } from "react";
import { Pencil, Search, Trash2, X } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { toast } from "sonner";

type Props = { customerId: number; selectedAddressId?: number; onSelect: (address: { id: number; recipientName: string; phone: string; address: string }) => void };

export default function CustomerAddressBook({ customerId, selectedAddressId, onSelect }: Props) {
  const [search, setSearch] = useState("");
  const [editingId, setEditingId] = useState<number | null>(null);
  const [draft, setDraft] = useState({ recipientName: "", phone: "", address: "", isDefault: false });
  const query = trpc.store.customerShippingAddresses.useQuery({ customerId }, { enabled: customerId > 0 });
  const update = trpc.store.updateCustomerShippingAddress.useMutation({ onSuccess: () => { toast.success("Đã cập nhật địa chỉ"); setEditingId(null); query.refetch(); }, onError: e => toast.error(e.message) });
  const remove = trpc.store.deleteCustomerShippingAddress.useMutation({ onSuccess: () => { toast.success("Đã xóa địa chỉ"); query.refetch(); }, onError: e => toast.error(e.message) });
  const addresses = useMemo(() => (query.data || []).filter(item => `${item.recipientName} ${item.phone} ${item.address}`.toLocaleLowerCase("vi").includes(search.trim().toLocaleLowerCase("vi"))), [query.data, search]);
  return <div className="mt-3 rounded-xl border border-indigo-100 bg-white p-3"><div className="flex items-center justify-between gap-2"><p className="text-[10px] font-black uppercase tracking-wide text-indigo-800">Quản lý sổ địa chỉ</p>{search && <button type="button" onClick={() => setSearch("")} className="text-slate-400 hover:text-slate-700" aria-label="Xóa tìm kiếm"><X className="h-3.5 w-3.5" /></button>}</div><div className="relative mt-2"><Search className="pointer-events-none absolute left-2.5 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-slate-400" /><Input value={search} onChange={e => setSearch(e.target.value)} className="h-9 pl-8 text-xs" placeholder="Tìm người nhận, số điện thoại hoặc địa chỉ…" aria-label="Tìm địa chỉ khách" /></div><div className="mt-2 space-y-2">{addresses.map(address => editingId === address.id ? <div key={address.id} className="rounded-lg border border-indigo-200 bg-indigo-50/40 p-2"><div className="grid gap-2 sm:grid-cols-2"><Input value={draft.recipientName} onChange={e => setDraft(v => ({ ...v, recipientName: e.target.value }))} placeholder="Họ tên" /><Input value={draft.phone} onChange={e => setDraft(v => ({ ...v, phone: e.target.value }))} placeholder="Số điện thoại" /></div><Textarea value={draft.address} onChange={e => setDraft(v => ({ ...v, address: e.target.value }))} className="mt-2 min-h-16 text-xs" placeholder="Địa chỉ chi tiết" /><div className="mt-2 flex gap-2"><Button type="button" size="sm" className="bg-indigo-700 text-white" disabled={update.isPending} onClick={() => update.mutate({ customerId, id: address.id, data: { recipientName: draft.recipientName.trim(), phone: draft.phone.trim(), address: draft.address.trim(), isDefault: draft.isDefault } })}>Lưu</Button><Button type="button" size="sm" variant="outline" onClick={() => setEditingId(null)}>Hủy</Button></div></div> : <div key={address.id} className={`flex items-start gap-2 rounded-lg border p-2 ${selectedAddressId === address.id ? "border-indigo-400 bg-indigo-50/40" : "border-slate-200"}`}><button type="button" className="min-w-0 flex-1 text-left" onClick={() => onSelect(address)}><p className="truncate text-xs font-black text-slate-900">{address.isDefault ? "Mặc định · " : ""}{address.recipientName} · {address.phone}</p><p className="mt-0.5 line-clamp-2 text-[11px] text-slate-500">{address.address}</p></button><Button type="button" size="icon" variant="ghost" className="h-7 w-7 text-indigo-700" aria-label="Sửa địa chỉ" onClick={() => { setEditingId(address.id); setDraft({ recipientName: address.recipientName, phone: address.phone, address: address.address, isDefault: address.isDefault }); }}><Pencil className="h-3.5 w-3.5" /></Button><Button type="button" size="icon" variant="ghost" className="h-7 w-7 text-rose-700" aria-label="Xóa địa chỉ" disabled={remove.isPending} onClick={() => { if (window.confirm("Xóa địa chỉ này khỏi sổ địa chỉ của khách?")) remove.mutate({ customerId, id: address.id }); }}><Trash2 className="h-3.5 w-3.5" /></Button></div>)}{!query.isLoading && !addresses.length && <p className="py-2 text-[11px] text-slate-500">Không có địa chỉ phù hợp.</p>}</div></div>;
}
